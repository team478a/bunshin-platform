import { retryBadgeLineDeliveryResponse } from '../../../../../../src/http/badge-line-delivery-retries';

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';

export const POST = retryBadgeLineDeliveryResponse;
