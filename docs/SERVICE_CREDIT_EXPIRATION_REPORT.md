# サービス別画像作成回数の期限切れ処理

期限付きで付与された画像作成回数は、内部の定期実行エンドポイントで失効します。

- 付与履歴を古い順に読み、画像作成で既に使われた分を先に差し引く。
- 使われずに残った期限付きの分だけを失効する。
- 失効は台帳へ `EXPIRE` として追記し、過去の付与・消費・返却履歴は変更しない。
- 同じ付与分は冪等キーにより二重で失効しない。
- サービス、参加情報、ユーザーごとの口座だけを更新する。

定期実行は `POST /api/internal/service-credits/expire` をCRON認証付きで呼び出します。
