# 活動継続 J4-A 運用KPI 実装報告

## 完成した範囲

- 管理画面の運用レポートへ「初めの7日間で3回投稿」を追加した。
- 登録後7日間の観測を完了した利用者だけを分母にする。
- 実際の投稿完了を表す`PostRecord`だけを分子に使用する。
- 同じ集計値を既存のUTF-8 CSVへ追加した。

投稿本文、投稿URL、Knowledge、Memory、秘密情報は画面・CSVへ出力しない。

## 後続範囲

J4-Bで運用ルール、ユーザー・Group別進捗、テスト利用者除外、変更監査、監視を扱う。
